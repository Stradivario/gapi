commands:
  cli:
    init: git pull --recurse-submodules
  version:
    patch:
      - for d in ./*/ ; do (cd "$d" && npm version patch && git push && npm publish --update-readme --access public); done && gapi repo commit >/dev/null 2>&1
  repo:
    commit:
      - npm version patch
      - gapi npm publish
      - git push
  npm:
    publish: npm publish --update-readme --access public